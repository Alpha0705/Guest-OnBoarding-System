<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Guest</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #f8f9fa;
            padding: 20px;
        }

        h1 {
            text-align: center;
            margin-bottom: 20px;
            font-weight: bold;
        }

        form {
            background-color: #ffffff;
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 600px;
            margin: 0 auto;
        }

        label {
            font-weight: bold;
            display: block;
            margin-top: 15px;
        }

        input {
            width: 100%;
            padding: 8px;
            margin-top: 5px;
            border: 1px solid #ced4da;
            border-radius: 4px;
        }

        button {
            display: block;
            width: 100%;
            padding: 10px;
            margin-top: 20px;
            background-color: #007bff;
            color: #ffffff;
            border: none;
            border-radius: 4px;
            font-size: 16px;
        }

        button:hover {
            background-color: #0056b3;
        }
    </style>
</head>

<body>
    <h1>Edit Guest</h1>
    <form action="/admin/guests/<%= guest.hotelId %>/edit/<%= guest._id %>" method="POST">
        <div class="mb-3">
            <label for="fullName">Full Name:</label>
            <input type="text" id="fullName" name="fullName" class="form-control" value="<%= guest.fullName %>" required>
        </div>
        <div class="mb-3">
            <label for="mobileNumber">Mobile Number:</label>
            <input type="text" id="mobileNumber" name="mobileNumber" class="form-control" value="<%= guest.mobileNumber %>" required>
        </div>
        <div class="mb-3">
            <label for="address">Address:</label>
            <input type="text" id="address" name="address" class="form-control" value="<%= guest.address %>" required>
        </div>
        <div class="mb-3">
            <label for="purposeOfVisit">Purpose of Visit:</label>
            <input type="text" id="purposeOfVisit" name="purposeOfVisit" class="form-control" value="<%= guest.purposeOfVisit %>" required>
        </div>
        <div class="mb-3">
            <label>Stay Dates:</label>
            <div class="d-flex gap-2">
                <input type="date" name="stayDates[from]" class="form-control" value="<%= guest.stayDates.from.toISOString().substring(0, 10) %>" required>
                <input type="date" name="stayDates[to]" class="form-control" value="<%= guest.stayDates.to.toISOString().substring(0, 10) %>" required>
            </div>
        </div>
        <div class="mb-3">
            <label for="emailId">Email ID:</label>
            <input type="email" id="emailId" name="emailId" class="form-control" value="<%= guest.emailId %>" required>
        </div>
        <div class="mb-3">
            <label for="idProofNumber">ID Proof Number:</label>
            <input type="text" id="idProofNumber" name="idProofNumber" class="form-control" value="<%= guest.idProofNumber %>" required>
        </div>
        <button type="submit" class="btn btn-primary">Update</button>
    </form>

    <!-- Bootstrap Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
